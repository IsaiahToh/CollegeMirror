# Backend origin for API requests. Leave unset in dev — the Vite proxy
# forwards /examples, /generate, /jobs and /health to http://localhost:8000.
# For production builds, set to the deployed backend origin:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
